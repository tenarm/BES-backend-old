[project]
name = "inventory"
version = "0.1.0"
description = "Inventory Extension Module"
requires-python = ">=3.10"
dependencies = [
    "core"
]

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["inventory"]
