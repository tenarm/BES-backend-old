[project]
name = "finance"
version = "0.1.0"
description = "Finance Extension Module"
requires-python = ">=3.10"
dependencies = [
    "core"
]

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["finance"]
