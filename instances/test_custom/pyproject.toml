[project]
name = "test-custom"
version = "0.1.0"
description = "Test Custom Client Instance"
requires-python = ">=3.10"
dependencies = [
    "sales",
    "settings",
]

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["test_custom"]
